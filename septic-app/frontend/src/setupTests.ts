/**
 * Loaded automatically by react-scripts before every test file.
 *
 * This line is the whole component test harness. It exists because for the life of this
 * repository the frontend had no way to assert anything about what it renders, and that is
 * why seven requirements about what a driver *sees* were held at 🟡 with a `T-DRV-XXb` todo
 * beside them rather than ✅: the payload was proven against the database row it came from,
 * and nothing had ever looked at the card.
 */
import '@testing-library/jest-dom';
